<div class="container">
	<div class="row">
		<div class="col-sm-12">
			<h1><%= @form.title %></h1>
			<ol>
				<% @form.questions.each do |question| %>
					<li><%= question.body %></li>
				<% end %>
			</ol>
		</div>
	</div>
</div>